---
phase: 01-visual-foundation-performance
plan: 03
type: execute
wave: 2
depends_on:
  - 01-01
  - 01-02
files_modified:
  - components/sections/Hero.tsx
  - components/sections/CoastalExpertise.tsx
  - components/sections/ServicesGrid.tsx
  - components/sections/WhyBreeze.tsx
autonomous: true
requirements:
  - DESIGN-02
  - DESIGN-05
must_haves:
  truths:
    - "Hero section displays a real project photo as background with priority loading for LCP optimization"
    - "Coastal NC visual storytelling is present through imagery references, language, and color palette usage"
    - "Hero image uses next/image with fill, priority, sizes='100vw', and descriptive alt text"
    - "Section components use motion-safe brand animations for entrance effects"
  artifacts:
    - path: "components/sections/Hero.tsx"
      provides: "Hero with real photo background and coastal messaging"
      contains: "priority"
    - path: "components/sections/CoastalExpertise.tsx"
      provides: "Enhanced coastal storytelling section"
    - path: "components/sections/ServicesGrid.tsx"
      provides: "Service cards with motion-safe entrance animations"
    - path: "components/sections/WhyBreeze.tsx"
      provides: "Why Breeze section with coastal brand identity"
  key_links:
    - from: "components/sections/Hero.tsx"
      to: "/images/hero/"
      via: "next/image with priority"
      pattern: "Image.*priority.*hero"
    - from: "components/sections/ServicesGrid.tsx"
      to: "app/globals.css"
      via: "motion-safe:animate-breeze-in class"
      pattern: "motion-safe"
---

<objective>
Transform the hero and key homepage sections to establish the coastal NC visual identity with real project photography.

Purpose: The hero is the first thing visitors see -- it sets the tone for the entire brand. Currently it's a solid navy gradient with cloud SVGs. Adding a real roofing photo background (with fallback to current gradient) creates the "premium local contractor" feel that distinguishes Breeze from generic templates. Applying brand animations to section components reinforces the "Breeze" identity.

Output: Photo-ready hero section, coastal-enhanced content sections with brand animations.
</objective>

<execution_context>
@C:/Users/NickK/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/NickK/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-visual-foundation-performance/01-RESEARCH.md

<interfaces>
From components/sections/Hero.tsx (current props):
```tsx
interface HeroProps {
  headline?: string;
  subheadline?: string;
  primaryCTA?: HeroCTA;
  secondaryCTA?: HeroCTA;
  trustItems?: string[];
  backgroundImage?: string;  // Already accepts image prop but not used
}
```

From app/globals.css (animations from Plan 01):
```css
@theme inline {
  --animate-breeze-in: breeze-in 0.6s ease-out;
  --animate-fade-in: fade-in 0.3s ease-out;
  --animate-slide-up: slide-up 0.4s ease-out;
}
```

Design token colors available as Tailwind classes:
- bg-navy, text-navy, bg-sky, text-sky, bg-amber, text-amber
- bg-gray-50, bg-gray-100, text-gray-600, text-gray-800, text-gray-900
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enhance Hero with photo background support and coastal identity</name>
  <files>components/sections/Hero.tsx</files>
  <action>
Update `components/sections/Hero.tsx` to support a real photo background while keeping the gradient fallback:

1. Add `import Image from 'next/image'` at top

2. The `backgroundImage` prop already exists in `HeroProps` but is unused. Wire it up:
   - When `backgroundImage` is provided: render a `next/image` with `fill`, `priority`, `sizes="100vw"`, `className="object-cover"`, and a descriptive `alt` prop (add `backgroundAlt?: string` to HeroProps, default to "Breeze Roofing project in Wilmington NC")
   - Add a dark overlay div (`absolute inset-0 bg-navy/60`) on top of the image so white text remains readable
   - When `backgroundImage` is NOT provided: keep the current gradient background (`bg-gradient-to-br from-navy via-navy to-navy-light`) and cloud animations exactly as they are

3. Keep ALL existing content (headline, subheadline, CTAs, trust bar) unchanged

4. Add `motion-safe:animate-fade-in` to the headline h1 element
5. Add `motion-safe:animate-slide-up` with a slight delay style to the subheadline paragraph (use inline style `animationDelay: '0.2s'` and add `animate-fill-mode: backwards` or use `opacity-0 motion-safe:animate-slide-up` pattern)

6. Ensure the component works in BOTH modes:
   - Default (no backgroundImage): gradient + clouds, identical to current appearance
   - With backgroundImage: photo + overlay, premium feel

Do NOT change the existing Cloud component or remove any cloud animation code.
Do NOT change prop defaults or trust items.
  </action>
  <verify>
    <automated>npx next build 2>&1 | tail -5</automated>
  </verify>
  <done>Hero.tsx renders with gradient fallback by default. When backgroundImage prop provided, displays next/image with priority. Headline and subheadline have motion-safe entrance animations. All existing functionality preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Add motion-safe animations and coastal references to homepage sections</name>
  <files>components/sections/CoastalExpertise.tsx, components/sections/ServicesGrid.tsx, components/sections/WhyBreeze.tsx</files>
  <action>
Enhance three homepage section components with brand animations and stronger coastal NC identity:

**ServicesGrid.tsx:**
- Add `motion-safe:animate-breeze-in` to each service card wrapper
- Stagger animation delays using inline `style={{ animationDelay: '{index * 0.1}s' }}` and `opacity-0 motion-safe:animate-breeze-in` pattern (cards start invisible, animate in sequentially)
- Add `motion-safe:hover:-translate-y-1 motion-safe:transition-transform` to card hover states
- Add `motion-reduce:opacity-100` to ensure cards are visible immediately for reduced-motion users
- Keep all existing content, links, and structure unchanged

**CoastalExpertise.tsx:**
- Read the current file first to understand its structure
- Add `motion-safe:animate-fade-in` to the section container or key content blocks
- Ensure coastal NC language is prominent: if the section doesn't already reference specific coastal concerns (hurricane-force winds, salt air corrosion, coastal building codes, flood zone considerations), enhance the descriptive text to include these
- Do NOT rewrite existing content that already has good coastal references -- only enhance where generic

**WhyBreeze.tsx:**
- Read the current file first to understand its structure
- Add `motion-safe:animate-slide-up` to feature/benefit cards or list items
- Add staggered animation delays if the section has multiple items
- Ensure `motion-reduce:opacity-100` fallback on animated elements
- If the section has icons or visual elements, add `motion-safe:transition-transform motion-safe:hover:scale-105` for subtle hover feedback

For ALL three files:
- Do NOT change routing, links, or data imports
- Do NOT change the HTML structure (only add/modify className attributes and potentially style attributes)
- Ensure every animated element has a `motion-reduce:opacity-100` fallback
  </action>
  <verify>
    <automated>npx next build 2>&1 | tail -5</automated>
  </verify>
  <done>ServicesGrid cards have staggered breeze-in entrance. CoastalExpertise has fade-in and enhanced coastal language. WhyBreeze has slide-up animations. All animations respect reduced-motion. Build succeeds.</done>
</task>

</tasks>

<verification>
- `npx next build` -- succeeds
- `grep -n "motion-safe" components/sections/Hero.tsx components/sections/ServicesGrid.tsx components/sections/CoastalExpertise.tsx components/sections/WhyBreeze.tsx` -- all files contain motion-safe classes
- `grep -n "priority" components/sections/Hero.tsx` -- priority prop present on Image
- `grep -n "motion-reduce" components/sections/ServicesGrid.tsx` -- reduced-motion fallback present
</verification>

<success_criteria>
- Hero supports real photo background via backgroundImage prop with next/image priority loading
- Hero falls back gracefully to gradient + clouds when no image provided
- ServicesGrid, CoastalExpertise, and WhyBreeze have motion-safe brand animations
- All animations have motion-reduce fallbacks
- Coastal NC visual storytelling enhanced in content sections
- Build succeeds, no regressions
</success_criteria>

<output>
After completion, create `.planning/phases/01-visual-foundation-performance/01-03-SUMMARY.md`
</output>
