---
phase: 01-visual-foundation-performance
plan: 04
type: execute
wave: 2
depends_on:
  - 01-01
  - 01-02
files_modified:
  - components/sections/ProjectGallery.tsx
  - lib/data/projects.ts
autonomous: false
requirements:
  - DESIGN-03
must_haves:
  truths:
    - "Project gallery renders real project data with before/after image support"
    - "Gallery images use next/image with proper aspect ratios to prevent CLS"
    - "Gallery gracefully handles placeholder state when real photos not yet available"
    - "User can visually verify the gallery looks professional and on-brand"
  artifacts:
    - path: "components/sections/ProjectGallery.tsx"
      provides: "Before/after gallery using next/image with CLS prevention"
      contains: "aspect-"
    - path: "lib/data/projects.ts"
      provides: "Typed project data with before/after image paths"
      contains: "export"
  key_links:
    - from: "components/sections/ProjectGallery.tsx"
      to: "lib/data/projects.ts"
      via: "import projects array"
      pattern: "import.*projects"
    - from: "components/sections/ProjectGallery.tsx"
      to: "next/image"
      via: "Image component for CLS prevention"
      pattern: "Image.*fill.*aspect"
---

<objective>
Build a data-driven before/after project gallery that showcases Brett's work with proper image optimization.

Purpose: DESIGN-03 requires a before/after gallery showing real projects. The current ProjectGallery.tsx is entirely placeholder (6 dashed-border boxes saying "Coming Soon"). This plan replaces it with a real data-driven component that can display project photos when available and shows professional placeholder cards when photos haven't been provided yet.

Output: Typed project data file, responsive gallery component with next/image, visual verification checkpoint.
</objective>

<execution_context>
@C:/Users/NickK/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/NickK/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-visual-foundation-performance/01-RESEARCH.md

<interfaces>
From components/sections/ProjectGallery.tsx (current -- all placeholder):
```tsx
export function ProjectGallery() {
  const placeholders = Array.from({ length: 6 });
  return (
    <section className="py-16 md:py-20 bg-white">
      {/* Grid of 6 dashed-border placeholder boxes */}
    </section>
  );
}
```

From app/globals.css (animation tokens from Plan 01):
```css
--animate-breeze-in: breeze-in 0.6s ease-out;
```

Image directory (from Plan 02):
- public/images/projects/ (empty, awaiting real photos from Brett)
- public/images/services/ (relocated service images available)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create project data file and rebuild gallery component</name>
  <files>lib/data/projects.ts, components/sections/ProjectGallery.tsx</files>
  <action>
1. Create `lib/data/projects.ts`:
- Define a `Project` interface:
  ```typescript
  export interface Project {
    id: number;
    title: string;
    location: string;
    description: string;
    serviceType: string;
    beforeImage?: string;  // Optional -- not all projects have before photos
    afterImage?: string;   // Optional -- photos come from Brett
    featured: boolean;
  }
  ```
- Export a `projects` array with 6 real project entries representing realistic Breeze Roofing work. Use real locations from the service areas (Hampstead, Wrightsville Beach, Leland, Carolina Beach, Southport, Wilmington). Include varied service types (roof replacement, storm damage repair, new construction, Fortified roof install).
- Leave `beforeImage` and `afterImage` as `undefined` for now (photos will come from Brett). This is intentional -- the component handles the missing-image state gracefully.

2. Rewrite `components/sections/ProjectGallery.tsx`:
- Import `Image` from `next/image`
- Import `projects` from `@/lib/data/projects`
- Import `Link` from `next/link`
- Keep the same section heading ("Our Work Across the Cape Fear Coast") and overall structure
- Replace the placeholder grid with a real data-driven grid:
  - `grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6`
  - For each project, render a card with:
    - `relative aspect-[4/3] overflow-hidden rounded-xl` container (prevents CLS)
    - If `afterImage` exists: `<Image src={project.afterImage} alt={project.title} fill className="object-cover" sizes="(max-width: 640px) 100vw, (max-width: 1024px) 50vw, 33vw" />`
    - If no `afterImage`: render a professional placeholder card with the project location icon, title, and "Photos coming soon" text (use bg-gray-50 with the dashed border pattern, but make it look polished, not obviously "under construction")
    - Below the image/placeholder: project title (h3, font-semibold), location (text-sm text-gray-600), description (text-sm text-gray-500, line-clamp-2)
  - Add `motion-safe:animate-breeze-in` with staggered delays to cards
  - Add `motion-reduce:opacity-100` fallback
  - Add `group` class and `motion-safe:group-hover:scale-105 transition-transform duration-300` on the image for hover zoom effect
- Keep the "See More Projects" CTA link at the bottom

Do NOT add any fake/stock images. The component must work cleanly with undefined image paths.
  </action>
  <verify>
    <automated>npx next build 2>&1 | tail -5</automated>
  </verify>
  <done>ProjectGallery renders 6 project cards from data file. Cards show professional placeholders when images undefined. Aspect ratio containers prevent CLS. Motion-safe animations applied. Build succeeds.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of Phase 1 visual foundation</name>
  <files>N/A</files>
  <action>
Human visually verifies the complete Phase 1 output across all 4 plans: design tokens, image infrastructure, hero enhancements, animated sections, and project gallery.
  </action>
  <verify>Human confirms visual quality meets brand standards</verify>
  <done>User approves the visual foundation or provides specific feedback for fixes</done>
  <what-built>
Phase 1 visual foundation: design tokens with brand animations, image infrastructure (AVIF, organized directories, monitoring), hero with photo support, animated homepage sections, and data-driven project gallery.
  </what-built>
  <how-to-verify>
1. Run `npm run dev` and open http://localhost:3000
2. **Homepage hero:** Verify gradient + cloud animation still works (no backgroundImage provided yet). Check that headline has a subtle fade-in animation on page load.
3. **Services grid:** Scroll to services section. Cards should animate in with staggered "breeze-in" effect. Hover over cards -- subtle lift animation.
4. **Project gallery:** Scroll to gallery section. Should show 6 professional placeholder cards with project names and locations (Hampstead, Wrightsville Beach, etc.). No broken images.
5. **Reduced motion:** In Chrome DevTools > Rendering > "Emulate CSS media feature prefers-reduced-motion" > set to "reduce". Reload page. All animations should be disabled; content should be immediately visible.
6. **Build check:** Confirm `npm run build` succeeds with no errors.
7. **Layout footer:** Scroll to bottom -- verify Vercel Analytics and SpeedInsights aren't visually disrupting anything (they're invisible components).
  </how-to-verify>
  <resume-signal>Type "approved" or describe any visual issues to fix</resume-signal>
</task>

</tasks>

<verification>
- `npx next build` -- full build succeeds
- `npx vitest run --reporter=verbose` -- all design token tests pass
- Visual verification of animations, gallery, and reduced-motion behavior
</verification>

<success_criteria>
- Project gallery displays 6 data-driven cards with professional placeholder state
- Gallery images use aspect-[4/3] containers for CLS prevention
- Gallery uses next/image when photos are available
- Motion-safe animations on gallery cards with reduced-motion fallback
- Human visually confirms the overall Phase 1 visual quality
</success_criteria>

<output>
After completion, create `.planning/phases/01-visual-foundation-performance/01-04-SUMMARY.md`
</output>
