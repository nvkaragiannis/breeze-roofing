---
phase: 01-visual-foundation-performance
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/globals.css
  - vitest.config.ts
  - package.json
  - tests/design-tokens.test.ts
autonomous: true
requirements:
  - DESIGN-06
  - DESIGN-04
  - DESIGN-01
must_haves:
  truths:
    - "Design tokens (colors, spacing, typography) are defined centrally and generate Tailwind utility classes"
    - "Custom brand animations exist with motion-safe/motion-reduce variants"
    - "Test infrastructure validates design system consistency"
  artifacts:
    - path: "app/globals.css"
      provides: "Expanded @theme design tokens and custom @keyframes animations"
      contains: "@theme inline"
    - path: "vitest.config.ts"
      provides: "Test framework configuration for Next.js 16"
    - path: "tests/design-tokens.test.ts"
      provides: "Validates design token CSS variables exist"
    - path: "package.json"
      provides: "vitest and testing-library dependencies"
      contains: "vitest"
  key_links:
    - from: "app/globals.css"
      to: "all components"
      via: "Tailwind @theme CSS variables"
      pattern: "--color-navy|--color-sky|--color-amber|--animate-"
---

<objective>
Establish the design system foundation and test infrastructure for Phase 1.

Purpose: All subsequent plans depend on consistent design tokens and testable code. This plan expands the existing @theme tokens with additional brand-specific values (shadows, border radii, animation keyframes) and sets up Vitest so all future tasks have automated verification.

Output: Enhanced globals.css with full design token system, custom Breeze brand animations, and working test infrastructure.
</objective>

<execution_context>
@C:/Users/NickK/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/NickK/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-visual-foundation-performance/01-RESEARCH.md

<interfaces>
<!-- Current globals.css @theme tokens (will be expanded) -->
From app/globals.css:
```css
@theme inline {
  --color-navy: #2A6895;
  --color-navy-light: #3A7DAD;
  --color-sky: #7BA7BC;
  --color-sky-light: #A3C4D4;
  --color-amber: #C6A355;
  --color-amber-hover: #B08F3E;
  --color-emergency: #A91A1E;
  --color-success: #2D7D32;
  /* ... grays and font-sans ... */
}
```

From app/globals.css (existing animation):
```css
@keyframes drift {
  from { left: -20%; }
  to { left: 120%; }
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install test infrastructure and configure Vitest</name>
  <files>package.json, vitest.config.ts</files>
  <action>
Install test dependencies:
```bash
npm install -D vitest @vitejs/plugin-react jsdom @testing-library/react @testing-library/jest-dom
```

Create `vitest.config.ts` at project root:
- Use `@vitejs/plugin-react` plugin
- Set environment to `jsdom`
- Set `include` to `["tests/**/*.test.{ts,tsx}"]`
- Set `css` to `true` (needed for design token tests)
- Add path alias `@/` pointing to project root (matching tsconfig)

Add `"test": "vitest"` script to package.json scripts.

Do NOT install happy-dom (jsdom is sufficient for this project).
  </action>
  <verify>
    <automated>npx vitest run --reporter=verbose 2>&1 | head -20</automated>
  </verify>
  <done>vitest.config.ts exists, `npm test -- --run` executes without config errors (0 tests found is OK at this stage)</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expand design tokens and add brand animations</name>
  <files>app/globals.css, tests/design-tokens.test.ts</files>
  <behavior>
    - Test 1: @theme block contains all required color tokens (navy, sky, amber, emergency, success, grays)
    - Test 2: @theme block contains animation tokens (breeze-in, fade-in, slide-up)
    - Test 3: globals.css contains motion-safe keyframes for brand animations
    - Test 4: No animation runs without motion-safe context (drift keyframes have motion-safe consideration)
  </behavior>
  <action>
Write `tests/design-tokens.test.ts` first (RED phase):
- Read `app/globals.css` as a string using `fs.readFileSync`
- Assert the file contains expected @theme tokens: all color variables, animation variables
- Assert keyframes exist: `breeze-in`, `fade-in`, `slide-up`
- Assert the file contains `motion-safe` or `motion-reduce` references in comments documenting usage

Run tests -- they should FAIL because new animation tokens don't exist yet.

Then update `app/globals.css` (GREEN phase):
- Keep all existing @theme tokens unchanged
- Add inside `@theme inline` block:
  - `--animate-breeze-in: breeze-in 0.6s ease-out` (signature brand animation -- elements float in like a breeze)
  - `--animate-fade-in: fade-in 0.3s ease-out` (simple fade for content sections)
  - `--animate-slide-up: slide-up 0.4s ease-out` (card/section entrance)
- Add `@keyframes breeze-in` (opacity 0 + translateY(20px) + scale(0.95) -> opacity 1 + translateY(0) + scale(1))
- Add `@keyframes fade-in` (opacity 0 -> 1)
- Add `@keyframes slide-up` (translateY(20px) + opacity 0 -> translateY(0) + opacity 1)
- Keep existing `@keyframes drift` unchanged
- Add a CSS comment block documenting that all animations should be used with `motion-safe:` Tailwind variant in components

Do NOT remove or modify any existing CSS. Only add new tokens and keyframes.
  </action>
  <verify>
    <automated>npx vitest run tests/design-tokens.test.ts --reporter=verbose</automated>
  </verify>
  <done>All design token tests pass. globals.css has expanded @theme with animation tokens. Existing styles untouched.</done>
</task>

</tasks>

<verification>
- `npx vitest run --reporter=verbose` -- all tests pass
- `npx next build` -- build succeeds with new globals.css
</verification>

<success_criteria>
- Vitest configured and running with jsdom environment
- Design tokens expanded with 3 brand animation keyframes
- Test file validates token presence
- Existing cloud drift animation and all color tokens preserved
- Build still succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/01-visual-foundation-performance/01-01-SUMMARY.md`
</output>
