---
phase: 01-visual-foundation-performance
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - next.config.ts
  - app/layout.tsx
  - public/images/hero/.gitkeep
  - public/images/projects/.gitkeep
  - public/images/services/.gitkeep
  - public/images/areas/.gitkeep
autonomous: true
requirements:
  - SEO-09
  - SEO-10
  - SEO-01
must_haves:
  truths:
    - "Images served in AVIF format with WebP fallback"
    - "Vercel Analytics and Speed Insights components render in root layout"
    - "Image directory structure organized for hero, projects, services, and areas"
    - "Existing service images moved to organized directory structure"
  artifacts:
    - path: "next.config.ts"
      provides: "AVIF + WebP image format configuration"
      contains: "image/avif"
    - path: "app/layout.tsx"
      provides: "Analytics and SpeedInsights components"
      contains: "SpeedInsights"
    - path: "public/images/services/"
      provides: "Organized service image directory"
  key_links:
    - from: "app/layout.tsx"
      to: "@vercel/analytics"
      via: "import and render"
      pattern: "Analytics.*vercel"
    - from: "app/layout.tsx"
      to: "@vercel/speed-insights"
      via: "import and render"
      pattern: "SpeedInsights.*vercel"
---

<objective>
Set up image optimization infrastructure and install performance monitoring.

Purpose: AVIF format reduces image size by 20-50% over WebP (improving LCP for SEO-01). Vercel Analytics and Speed Insights provide real-user Core Web Vitals monitoring. Organizing images into directories prevents file sprawl as real project photos are added.

Output: AVIF-enabled next.config.ts, Analytics/SpeedInsights in layout, organized image directories with existing images relocated.
</objective>

<execution_context>
@C:/Users/NickK/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/NickK/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-visual-foundation-performance/01-RESEARCH.md

<interfaces>
From app/layout.tsx (current structure):
```tsx
import { GoogleAnalytics } from "@next/third-parties/google";
import { getLocalBusinessSchema } from "@/lib/schema";

export default function RootLayout({ children }) {
  return (
    <html lang="en" className={`${inter.variable} h-full antialiased`}>
      <body className="min-h-full flex flex-col font-sans">
        <a href="#main-content" className="skip-to-content">Skip to main content</a>
        {children}
        <SchemaScript />
        {process.env.NEXT_PUBLIC_GA_ID && (
          <GoogleAnalytics gaId={process.env.NEXT_PUBLIC_GA_ID} />
        )}
      </body>
    </html>
  );
}
```

From next.config.ts (current):
```ts
images: {
  formats: ["image/webp"],
},
```

Current public/ images (flat structure):
- residential_roofing_pic.jpeg, commericla_roofing_pic.webp, emergency_repairs_pic.jpeg
- storm_damage_repair_pic.webp, roof_inspections_pic.webp, roof_replacement_pic.jpg
- new_construction_pic.webp, solar_ready_roofing_pic.webp, maintenance_programs.webp
- breeze_roofing_logo_white_background.jpeg
- Leaf+Solution+SELF+SHED+DEBRIS+USE-1920w.webp, SELF+SHEDDING+with+shingles+Xtreme+USE+copy-1920w.webp
- 56843992-0-new-wave-miter-1-1000x516-1920w.webp, momnt-web-banner-1650x320-statement.webp
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install monitoring packages and enable AVIF</name>
  <files>package.json, next.config.ts, app/layout.tsx</files>
  <action>
1. Install packages:
```bash
npm install @vercel/analytics @vercel/speed-insights
```

2. Update `next.config.ts`:
- Change `formats: ["image/webp"]` to `formats: ["image/avif", "image/webp"]`
- AVIF listed first so browsers that support it get smaller files; WebP is automatic fallback
- Do NOT change any other config (keep turbopack, headers, etc.)

3. Update `app/layout.tsx`:
- Add imports: `import { Analytics } from '@vercel/analytics/next'` and `import { SpeedInsights } from '@vercel/speed-insights/next'`
- Add `<Analytics />` and `<SpeedInsights />` as the LAST children inside `<body>`, after the GoogleAnalytics conditional
- Keep ALL existing code (SchemaScript, skip-to-content, GA conditional) unchanged
- These components auto-detect Vercel deployment; they render nothing in local dev (no env vars needed)
  </action>
  <verify>
    <automated>npx next build 2>&1 | tail -5</automated>
  </verify>
  <done>Build succeeds. layout.tsx contains Analytics and SpeedInsights imports/components. next.config.ts has AVIF format.</done>
</task>

<task type="auto">
  <name>Task 2: Organize image directory structure and relocate existing images</name>
  <files>public/images/hero/.gitkeep, public/images/projects/.gitkeep, public/images/services/, public/images/areas/.gitkeep, public/images/brand/</files>
  <action>
1. Create directory structure under `public/images/`:
   - `public/images/hero/` -- for hero/banner images (priority loaded)
   - `public/images/projects/` -- for before/after gallery photos
   - `public/images/services/` -- for service page images
   - `public/images/areas/` -- for area-specific landmark photos
   - `public/images/brand/` -- for logo and branding assets

2. Move existing service images to `public/images/services/`:
   - `residential_roofing_pic.jpeg` -> `public/images/services/residential_roofing_pic.jpeg`
   - `commericla_roofing_pic.webp` -> `public/images/services/commericla_roofing_pic.webp`
   - `emergency_repairs_pic.jpeg` -> `public/images/services/emergency_repairs_pic.jpeg`
   - `storm_damage_repair_pic.webp` -> `public/images/services/storm_damage_repair_pic.webp`
   - `roof_inspections_pic.webp` -> `public/images/services/roof_inspections_pic.webp`
   - `roof_replacement_pic.jpg` -> `public/images/services/roof_replacement_pic.jpg`
   - `new_construction_pic.webp` -> `public/images/services/new_construction_pic.webp`
   - `solar_ready_roofing_pic.webp` -> `public/images/services/solar_ready_roofing_pic.webp`
   - `maintenance_programs.webp` -> `public/images/services/maintenance_programs.webp`

3. Move brand assets to `public/images/brand/`:
   - `breeze_roofing_logo_white_background.jpeg` -> `public/images/brand/breeze_roofing_logo_white_background.jpeg`

4. Move product images to `public/images/services/` (leaf solutions products):
   - `Leaf+Solution+SELF+SHED+DEBRIS+USE-1920w.webp` -> `public/images/services/leaf-solution-self-shed-debris.webp`
   - `SELF+SHEDDING+with+shingles+Xtreme+USE+copy-1920w.webp` -> `public/images/services/leaf-solution-self-shedding-xtreme.webp`
   - `56843992-0-new-wave-miter-1-1000x516-1920w.webp` -> `public/images/services/leaf-solution-new-wave-miter.webp`
   - `momnt-web-banner-1650x320-statement.webp` -> `public/images/services/momnt-financing-banner.webp`

5. Add `.gitkeep` files to `public/images/hero/`, `public/images/projects/`, and `public/images/areas/` (empty dirs need this for git)

6. Keep `public/llms.txt` and `public/favicon.ico` in place (not images).

7. **CRITICAL:** Update ALL references to moved images across the entire codebase. Search for each old filename in all `.tsx` files and update the `src` path. Common locations:
   - `app/services/[service]/page.tsx` or `lib/data/services.ts` (service images)
   - `app/leaf-solutions/page.tsx` (leaf solution product images)
   - `app/financing/page.tsx` (momnt banner)
   - `components/layout/Header.tsx` or `app/layout.tsx` (logo)
   - Any component referencing `/residential_roofing_pic.jpeg` etc.
   
   Use grep to find all occurrences: `grep -rn "residential_roofing_pic\|commericla_roofing_pic\|emergency_repairs_pic\|storm_damage_repair_pic\|roof_inspections_pic\|roof_replacement_pic\|new_construction_pic\|solar_ready_roofing_pic\|maintenance_programs\|breeze_roofing_logo\|Leaf+Solution\|SELF+SHEDDING\|56843992\|momnt-web" app/ components/ lib/`
   
   Update each reference to the new `/images/services/`, `/images/brand/`, etc. path.
  </action>
  <verify>
    <automated>npx next build 2>&1 | tail -10</automated>
  </verify>
  <done>All images organized into subdirectories. All image references updated. Build succeeds with no broken image paths. Empty directories have .gitkeep files.</done>
</task>

</tasks>

<verification>
- `npx next build` -- succeeds, no image reference errors
- `ls public/images/services/` -- contains all relocated service images
- `grep -rn "Analytics\|SpeedInsights" app/layout.tsx` -- both present
- `grep "image/avif" next.config.ts` -- AVIF format configured
</verification>

<success_criteria>
- AVIF format enabled in next.config.ts (with WebP fallback)
- @vercel/analytics and @vercel/speed-insights installed and rendering in layout
- All existing images relocated to organized public/images/ subdirectories
- All image references in codebase updated to new paths
- Build succeeds cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/01-visual-foundation-performance/01-02-SUMMARY.md`
</output>
