---
phase: 02-conversion-architecture
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - components/sections/WarrantyCallout.tsx
  - components/sections/TimelineCallout.tsx
  - lib/data/services.ts
  - app/services/[service]/page.tsx
autonomous: true
requirements: [CONV-05, CONV-06]

must_haves:
  truths:
    - "User sees warranty/guarantee information on every service page"
    - "User sees expected project timeline with numbered steps on every service page"
  artifacts:
    - path: "components/sections/WarrantyCallout.tsx"
      provides: "Reusable warranty section component"
      exports: ["WarrantyCallout"]
      min_lines: 20
    - path: "components/sections/TimelineCallout.tsx"
      provides: "Reusable timeline section component"
      exports: ["TimelineCallout"]
      min_lines: 20
    - path: "lib/data/services.ts"
      provides: "Service data with warranty and timeline fields"
      contains: "warranties"
    - path: "app/services/[service]/page.tsx"
      provides: "Service page template with warranty and timeline sections"
      contains: "WarrantyCallout"
  key_links:
    - from: "app/services/[service]/page.tsx"
      to: "components/sections/WarrantyCallout.tsx"
      via: "import and render with service data"
      pattern: "WarrantyCallout.*warranties"
    - from: "app/services/[service]/page.tsx"
      to: "components/sections/TimelineCallout.tsx"
      via: "import and render with service data"
      pattern: "TimelineCallout.*duration"
---

<objective>
Add warranty/guarantee and project timeline sections to all service pages via reusable section components and data-driven service definitions.

Purpose: Users visiting service pages currently see no warranty information or expected project timelines, which are key conversion drivers for roofing services. Adding these builds trust and sets expectations.

Output: WarrantyCallout and TimelineCallout components, service data extended with warranty/timeline fields, service page template updated.
</objective>

<execution_context>
@C:/Users/NickK/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/NickK/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-conversion-architecture/02-RESEARCH.md

<interfaces>
<!-- Key types the executor needs. -->

From lib/data/services.ts (current Service interface):
```typescript
export interface Service {
  slug: string;
  title: string;
  shortTitle: string;
  description: string;
  icon: string;
  image?: { src: string; alt: string };
  featured?: boolean;
  showEmergency?: boolean;
  metaTitle: string;
  metaDescription: string;
  content: string;
  faqs: ServiceFAQ[];
}
```

From app/services/[service]/page.tsx (current section order):
```
article (title + image + content)
  -> "We Serve These Areas" section
  -> FAQ section
  -> EmergencyCTA (conditional on showEmergency)
  -> EstimateSection
```

Warranty and Timeline should be inserted AFTER the FAQ section and BEFORE EmergencyCTA.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create WarrantyCallout and TimelineCallout components</name>
  <files>components/sections/WarrantyCallout.tsx, components/sections/TimelineCallout.tsx</files>
  <action>
  Create WarrantyCallout.tsx as a server component (no "use client"):
  ```typescript
  interface WarrantyCalloutProps {
    title?: string;
    warranties: string[];
  }
  ```
  - Render a visually distinct section with `py-12 bg-sky/10 border-y border-sky/20` background
  - Left side: Shield icon in a navy circle (w-12 h-12 rounded-full bg-navy)
  - Right side: title (h2, text-2xl font-bold) and unordered list of warranties
  - Each warranty item: CheckCircle icon (w-5 h-5 text-navy) + text span
  - Use lucide-react for Shield and CheckCircle icons
  - Import from lucide-react, no other dependencies needed
  - Max width: max-w-3xl mx-auto with standard px-4 sm:px-6 lg:px-8 padding

  Create TimelineCallout.tsx as a server component (no "use client"):
  ```typescript
  interface TimelineStep {
    phase: string;
    time: string;
  }
  interface TimelineCalloutProps {
    title?: string;
    duration: string;
    steps: TimelineStep[];
  }
  ```
  - Render with `py-12 bg-amber/5 border-y border-amber/20` background (distinct from warranty section)
  - Left side: Clock icon in an amber circle (w-12 h-12 rounded-full bg-amber)
  - Right side: title (h2), total duration paragraph, numbered step grid
  - Each step: numbered circle (w-6 h-6 rounded-full bg-amber/20 text-amber), phase name (bold), time
  - Use lucide-react Clock icon
  - Max width: max-w-3xl mx-auto with standard padding
  </action>
  <verify>
    <automated>npx next build</automated>
  </verify>
  <done>WarrantyCallout and TimelineCallout components exist with correct props interfaces, styled distinctly from each other.</done>
</task>

<task type="auto">
  <name>Task 2: Extend Service data and integrate sections into service page template</name>
  <files>lib/data/services.ts, app/services/[service]/page.tsx</files>
  <action>
  Extend the Service interface in lib/data/services.ts with two new optional fields:
  ```typescript
  warranties?: string[];
  timeline?: {
    duration: string;
    steps: { phase: string; time: string }[];
  };
  ```

  Add warranty and timeline data to ALL 10 services in the services array. Use realistic roofing industry data:

  **Standard warranties (shared baseline, customize per service):**
  - "Lifetime workmanship guarantee on all installations"
  - "Manufacturer warranty coverage (25-50 years depending on material)"
  - "Annual courtesy inspection for the first 5 years"
  - "Storm damage priority service for warranty customers"

  **Service-specific customizations:**
  - Residential Roofing: Add "New roof certification letter for insurance providers"
  - Commercial Roofing: Replace "Lifetime" with "10-year workmanship guarantee"; add "Preventive maintenance program available"
  - Fortified Roof: Add "IBHS Fortified designation documentation for insurance discounts"
  - Roof Repair: Use shorter list: "1-year workmanship guarantee on repairs", "Material warranty on replaced components", "Follow-up inspection within 30 days"
  - Storm Damage: Add "Insurance claim documentation support", "Emergency tarping warranty"
  - Gutter: Customize for gutter-specific warranties
  - Metal Roofing: Emphasize longevity ("50-year manufacturer warranty")
  - Roof Inspection: No warranties needed (set to undefined or omit)
  - Emergency Services: "Emergency response within 2 hours" + repair warranties

  **Timelines per service (realistic for Wilmington NC roofing):**
  - Residential: duration "2-5 days", steps: Free Inspection (1 hour), Material Ordering (3-7 days), Installation (1-3 days), Final Inspection (same day)
  - Commercial: duration "1-3 weeks", longer steps
  - Roof Repair: duration "1-2 days", fewer steps
  - Storm Damage: duration "3-7 days", includes insurance coordination step
  - Emergency: duration "Same day - 48 hours", rapid response steps
  - Adapt other services similarly with realistic timelines

  Then update app/services/[service]/page.tsx:
  - Import WarrantyCallout and TimelineCallout from components/sections/
  - Insert them AFTER the FAQ section and BEFORE `{service.showEmergency && <EmergencyCTA />}`
  - Conditionally render: `{service.warranties && service.warranties.length > 0 && <WarrantyCallout warranties={service.warranties} />}`
  - Conditionally render: `{service.timeline && <TimelineCallout duration={service.timeline.duration} steps={service.timeline.steps} />}`
  </action>
  <verify>
    <automated>npx next build</automated>
  </verify>
  <done>All service pages show warranty and timeline sections with service-specific data. Pages without warranty data (like Roof Inspection) gracefully omit the sections. Build passes.</done>
</task>

</tasks>

<verification>
- `npx next build` succeeds with no errors
- Service pages render WarrantyCallout with Shield icon and warranty list
- Service pages render TimelineCallout with numbered steps
- Sections appear between FAQ and EmergencyCTA
- Services without warranty/timeline data gracefully omit sections
- Visual distinction between warranty (sky/blue) and timeline (amber) sections
</verification>

<success_criteria>
- WarrantyCallout renders on all applicable service pages with service-specific warranty data
- TimelineCallout renders on all applicable service pages with realistic timelines
- Sections are visually distinct and follow existing design patterns
- Service interface extended cleanly with optional fields (no breaking changes)
- Build passes with no TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/02-conversion-architecture/02-02-SUMMARY.md`
</output>
