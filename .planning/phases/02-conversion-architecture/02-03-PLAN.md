---
phase: 02-conversion-architecture
plan: 03
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - components/roofr/InstantEstimatorEmbed.tsx
  - app/estimate/page.tsx
autonomous: true
requirements: [CONV-02, CONV-03]

must_haves:
  truths:
    - "User can reach estimate request form within 1 click from any page"
    - "User on /estimate page sees Roofr instant estimator placeholder with proper mobile UX"
    - "Roofr embed handles postMessage with origin validation for security"
  artifacts:
    - path: "components/roofr/InstantEstimatorEmbed.tsx"
      provides: "Enhanced Roofr embed with iframe support and responsive container"
      exports: ["InstantEstimatorEmbed"]
    - path: "app/estimate/page.tsx"
      provides: "Estimate page with trust signals and enhanced Roofr embed"
  key_links:
    - from: "app/estimate/page.tsx"
      to: "components/roofr/InstantEstimatorEmbed.tsx"
      via: "import and render"
      pattern: "InstantEstimatorEmbed"
    - from: "components/roofr/InstantEstimatorEmbed.tsx"
      to: "https://app.roofr.com"
      via: "postMessage origin validation"
      pattern: "event\\.origin.*roofr"
---

<objective>
Enhance the Roofr instant estimator embed for production readiness and improve the estimate page conversion flow with trust signals and better mobile UX.

Purpose: The /estimate page is the primary conversion endpoint — every "Get Free Estimate" CTA across the entire site links here. The embed must handle Brett's Roofr code when provided, resize responsively on mobile, and the page must reinforce trust to prevent abandonment.

Output: Production-ready InstantEstimatorEmbed with iframe/embed support, enhanced estimate page with trust reinforcement.
</objective>

<execution_context>
@C:/Users/NickK/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/NickK/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-conversion-architecture/02-RESEARCH.md

<interfaces>
<!-- Key types the executor needs. -->

From components/roofr/InstantEstimatorEmbed.tsx (current):
```typescript
interface InstantEstimatorEmbedProps {
  className?: string;
}
// Currently: postMessage listener + placeholder div
// Needs: embedUrl prop for iframe src, responsive height
```

From app/estimate/page.tsx (current structure):
```
section: hero (h1 + InstantEstimatorEmbed)
section: "What Happens Next" (3-step grid)
section: "Common Questions" (FAQ accordion)
// No trust signals on this page currently
```

From components/sections/TrustBar.tsx (after Plan 01):
```typescript
interface TrustBarProps {
  variant?: "section" | "compact";
  className?: string;
}
// compact variant will be available from Plan 01
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Enhance InstantEstimatorEmbed with iframe src support and responsive container</name>
  <files>components/roofr/InstantEstimatorEmbed.tsx</files>
  <action>
  Enhance the existing InstantEstimatorEmbed component to support Brett's Roofr embed URL via a safe iframe approach (NOT dangerouslySetInnerHTML):

  Update the interface:
  ```typescript
  interface InstantEstimatorEmbedProps {
    embedUrl?: string;  // Roofr iframe src URL from Brett's dashboard
    className?: string;
  }
  ```

  Add state for dynamic height:
  - `const [height, setHeight] = useState(650);`
  - In the existing postMessage handler, add height handling: if `data.type === "resize" && typeof data.height === "number"`, call `setHeight(data.height)`

  Rendering logic:
  - If `embedUrl` is NOT provided (current state — Brett hasn't given code yet): render the existing placeholder with the calculator icon, "Roofr Instant Estimator" title, and "Brett's embed code goes here" subtitle. Keep this exactly as-is for now.
  - If `embedUrl` IS provided: render a safe iframe element:
    ```tsx
    <iframe
      src={embedUrl}
      title="Roofr Instant Roof Estimator"
      className="w-full rounded-xl border-0"
      style={{ height: `${height}px`, transition: "height 0.3s ease" }}
      allow="geolocation"
      loading="lazy"
    />
    ```
    This avoids dangerouslySetInnerHTML entirely. The iframe src approach is the standard secure pattern for third-party embeds.

  Keep the existing useEffect postMessage listener. The origin check `event.origin !== "https://app.roofr.com"` MUST remain for security.

  Add `useRef<HTMLDivElement>(null)` on the outer container for potential future use.

  The component must remain a client component ("use client") because of useState and useEffect.
  </action>
  <verify>
    <automated>npx next build</automated>
  </verify>
  <done>InstantEstimatorEmbed accepts optional embedUrl prop, renders placeholder when empty, renders secure iframe when provided. postMessage handler validates origin and handles resize events. Build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Enhance estimate page with trust signals and conversion optimization</name>
  <files>app/estimate/page.tsx</files>
  <action>
  Enhance the estimate page to reinforce trust and improve conversion:

  1. Add trust reinforcement below the h1 heading and description, before the InstantEstimatorEmbed:
     - Import TrustBar from `@/components/sections/TrustBar`
     - Add `<TrustBar variant="compact" className="justify-center mb-8 text-gray-600" />` between the description paragraph and the InstantEstimatorEmbed
     - This shows rating, certifications inline to build trust before the user engages with the estimator

  2. Add a "Why Breeze Roofing?" trust section between the "What Happens Next" steps and the FAQ:
     - Create a simple 2x2 or 4-column grid with trust points:
       - "Licensed & Insured" with Shield icon
       - "Fortified Certified" with Award icon
       - "4.9 Star Rating" with Star icon
       - "24/7 Emergency" with Clock icon
     - Use `bg-white` section with `py-12` padding
     - Each item: icon (w-8 h-8 text-navy) + title (font-semibold) + short description
     - Keep it simple — no new components needed, just a section in the page

  3. Add a stronger closing CTA after the FAQ section:
     - "Ready to Get Started?" heading
     - Brief paragraph about no obligation
     - Dual CTA: phone link (navy outlined) + "Get Free Estimate" anchor to top of page (amber solid)
     - Use `bg-navy text-white` section for visual impact

  Keep all existing content (steps, FAQs, phone number reference). Only ADD to the page, do not remove existing sections.
  </action>
  <verify>
    <automated>npx next build</automated>
  </verify>
  <done>Estimate page has compact TrustBar above estimator, trust grid section, and strong closing CTA. All existing content preserved. Build passes.</done>
</task>

</tasks>

<verification>
- `npx next build` succeeds with no errors
- InstantEstimatorEmbed renders placeholder when no embedUrl (current production state)
- InstantEstimatorEmbed ready to accept Brett's Roofr URL via safe iframe src
- Estimate page shows trust signals before estimator engagement
- Estimate page has trust grid and closing CTA
- All "Get Free Estimate" CTAs across site link to /estimate (verify in build output)
</verification>

<success_criteria>
- Roofr embed component is production-ready for Brett's embed URL
- Estimate page reinforces trust at multiple points in the conversion funnel
- postMessage handler maintains origin security validation
- Responsive mobile UX maintained (no horizontal scroll)
- Build passes with no TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/02-conversion-architecture/02-03-SUMMARY.md`
</output>
