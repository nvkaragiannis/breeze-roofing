---
phase: 03-seo-technical-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - app/sitemap.ts
  - app/page.tsx
  - app/services/page.tsx
  - app/areas/page.tsx
  - app/blog/page.tsx
  - app/estimate/page.tsx
  - app/about/page.tsx
  - app/contact/page.tsx
  - app/faq/page.tsx
  - app/financing/page.tsx
  - app/roofing-products/page.tsx
  - app/leaf-solutions/page.tsx
  - app/privacy-policy/page.tsx
  - app/terms/page.tsx
autonomous: true
requirements:
  - SEO-06
  - SEO-07
  - SEO-08

must_haves:
  truths:
    - "XML sitemap at /sitemap.xml includes all 43+ pages with realistic lastModified dates (not new Date())"
    - "Sitemap includes leaf-solutions page (currently missing)"
    - "Every page has unique meta title and description (no duplicates across any two pages)"
    - "All pages have canonical URLs set in metadata"
    - "Homepage has internal links to key service and area pages for crawlability"
  artifacts:
    - path: "app/sitemap.ts"
      provides: "Complete sitemap with fixed lastModified dates, all pages including leaf-solutions"
      contains: "leaf-solutions"
    - path: "app/page.tsx"
      provides: "Homepage with enhanced internal linking structure"
      contains: "SchemaScript"
  key_links:
    - from: "app/sitemap.ts"
      to: "lib/data/services.ts"
      via: "services array for service page URLs"
      pattern: "services\\.map"
    - from: "app/sitemap.ts"
      to: "lib/data/areas.ts"
      via: "areas array for area page URLs"
      pattern: "areas\\.map"
---

<objective>
Fix sitemap anti-patterns (new Date() on static pages, missing leaf-solutions page), verify metadata uniqueness across all 43+ pages, and ensure internal linking architecture supports crawlability.

Purpose: Ensure Google can discover, crawl, and properly index all pages with correct canonical signals and a complete, accurate sitemap.
Output: Production-ready sitemap.ts with fixed dates, verified unique metadata across all pages.
</objective>

<execution_context>
@C:/Users/NickK/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/NickK/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-seo-technical-foundation/03-RESEARCH.md

<interfaces>
<!-- Current sitemap structure -->
From app/sitemap.ts:
```typescript
import type { MetadataRoute } from "next";
import { services } from "@/lib/data/services";
import { areas } from "@/lib/data/areas";
import { getAllPosts } from "@/lib/blog";

const BASE_URL = "https://breezeroofingnc.com";
export default function sitemap(): MetadataRoute.Sitemap { ... }
```

<!-- Data sources for sitemap completeness -->
From lib/data/services.ts: services array with .slug property
From lib/data/areas.ts: areas array with .slug property
From lib/blog.ts: getAllPosts() returns BlogPost[] with .slug and .date
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix sitemap with realistic dates and complete page coverage</name>
  <files>app/sitemap.ts</files>
  <action>
  Rewrite app/sitemap.ts to fix three issues:

  1. **Replace `new Date()` with fixed dates** for all static pages. Use `new Date("2026-04-04")` as the last content update date (Phase 2 completion date). This is accurate since all pages were last modified during Phase 1-2 work. For blog posts, keep using `new Date(post.date)` which is already correct.

  2. **Add missing leaf-solutions page** to the static pages list. It currently exists at `app/leaf-solutions/page.tsx` but is not in the sitemap. Add it with:
     - url: `${BASE_URL}/leaf-solutions`
     - lastModified: `new Date("2026-04-04")`
     - changeFrequency: "monthly"
     - priority: 0.5

  3. **Review priority values** for accuracy (these are semantic documentation, Google ignores them, but keep them reasonable):
     - Homepage: 1.0
     - Areas listing: 0.8 (was 0.9, areas are important for local SEO)
     - Services listing: 0.8
     - Individual service pages: 0.8
     - Individual area pages: 0.8 (was 0.9)
     - Estimate: 0.8 (high conversion page)
     - Blog listing: 0.7
     - Individual blog posts: 0.7
     - About, FAQ, Contact, Financing, Roofing Products, Leaf Solutions: 0.5
     - Privacy Policy, Terms: 0.2

  Keep the same overall structure: staticPages array + servicePages map + areaPages map + blogPages map, then spread into return array.
  </action>
  <verify>
    <automated>cd "C:\Users\NickK\Desktop\Projects\Roofing\breeze_roofing" && npx next build</automated>
  </verify>
  <done>Sitemap uses fixed dates for static pages, includes leaf-solutions, blog posts use actual post dates. Build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Audit metadata uniqueness and verify canonical URLs</name>
  <files>
    app/page.tsx
    app/services/page.tsx
    app/areas/page.tsx
    app/blog/page.tsx
    app/estimate/page.tsx
    app/about/page.tsx
    app/contact/page.tsx
    app/faq/page.tsx
    app/financing/page.tsx
    app/roofing-products/page.tsx
    app/leaf-solutions/page.tsx
    app/privacy-policy/page.tsx
    app/terms/page.tsx
  </files>
  <action>
  Perform a metadata audit across all static pages and verify:

  1. **Unique titles:** Read every page's `export const metadata` and `generateMetadata` function. Verify no two pages share the same title. The root layout uses `title.template: "%s | Breeze Roofing"` so inner page titles get " | Breeze Roofing" appended. Verify inner page titles are descriptive and unique.

  2. **Unique descriptions:** Verify no two pages share the same meta description. Each should mention the specific page topic and Wilmington NC.

  3. **Canonical URLs present:** Already verified all pages have canonical URLs set (confirmed via grep). No changes needed here.

  4. **Homepage internal linking audit:** Review app/page.tsx to confirm it links to:
     - All service pages (via ServicesGrid component)
     - All area pages (via ServiceAreasMap component)
     - Blog posts (via BlogPreview component)
     - Estimate page (via EstimateSection component)
     These are already present via existing section components. Verify this is the case by reading the section components.

  **Expected finding:** The existing metadata is likely already unique since services use `service.metaTitle`, areas use `area.metaTitle`, and blog posts use `post.title`. Static pages each have hand-written titles. If any duplicates are found, fix them to be unique and descriptive.

  **If no changes needed:** Document the audit results in the SUMMARY. The value of this task is verification, not necessarily modification.

  **If changes needed:** Fix any duplicate titles/descriptions to be unique. Ensure each follows the pattern: "[Specific Topic] - Wilmington NC | Breeze Roofing" or similar unique format.
  </action>
  <verify>
    <automated>cd "C:\Users\NickK\Desktop\Projects\Roofing\breeze_roofing" && npx next build</automated>
  </verify>
  <done>All 43+ pages verified to have unique meta titles and descriptions. No duplicate metadata across any pages. Canonical URLs present on all pages. Internal linking from homepage reaches all major page categories.</done>
</task>

</tasks>

<verification>
1. `npx next build` completes successfully
2. Sitemap output (accessible at /sitemap.xml after build) includes all pages including leaf-solutions
3. No static page in sitemap uses today's dynamic date (all use fixed "2026-04-04" or similar)
4. Blog posts in sitemap use their actual publication dates
5. No two pages share the same meta title or description
6. All pages have canonical URLs in their metadata
</verification>

<success_criteria>
- Sitemap includes all 43+ pages (static + service + area + blog) including leaf-solutions
- Static page lastModified uses fixed dates, not new Date()
- All pages have unique meta title and unique meta description
- All pages have canonical URL set in metadata.alternates
- Homepage links to services, areas, blog, and estimate via existing section components
- Build passes with zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/03-seo-technical-foundation/03-02-SUMMARY.md`
</output>
