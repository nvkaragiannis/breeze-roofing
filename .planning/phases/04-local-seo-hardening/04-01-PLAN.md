---
phase: 04-local-seo-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/data/company.ts
  - lib/schema.ts
  - components/layout/Footer.tsx
autonomous: true
requirements:
  - LOCAL-01
  - LOCAL-02

must_haves:
  truths:
    - "NAP data in company.ts uses realistic placeholder values (street address, license number, year founded) instead of TBD markers"
    - "Every NAP display on the site imports from company.ts (no hardcoded phone numbers, addresses, or business names)"
    - "Schema openingHoursSpecification matches company.ts hours exactly"
    - "Schema geo coordinates have 5+ decimal places for GBP precision"
    - "Footer displays full street address from company.ts"
  artifacts:
    - path: "lib/data/company.ts"
      provides: "Complete NAP data with realistic placeholders replacing all TBD markers"
      contains: "street:"
    - path: "lib/schema.ts"
      provides: "RoofingContractor schema with foundingDate, priceRange, and paymentAccepted"
      contains: "foundingDate"
    - path: "components/layout/Footer.tsx"
      provides: "Full street address display"
      contains: "company.address.street"
  key_links:
    - from: "lib/schema.ts"
      to: "lib/data/company.ts"
      via: "import { company }"
      pattern: "company\\.address\\.street"
    - from: "components/layout/Footer.tsx"
      to: "lib/data/company.ts"
      via: "import { company }"
      pattern: "company\\.address"
---

<objective>
Complete NAP data in company.ts and ensure schema markup is fully aligned for Google Business Profile consistency.

Purpose: LOCAL-01 requires NAP consistency across the entire site. LOCAL-02 requires schema to match GBP exactly. Both depend on company.ts having complete, realistic data instead of placeholder markers.
Output: Complete company.ts with realistic placeholder values, enhanced schema with foundingDate and additional GBP properties, Footer showing full address.
</objective>

<execution_context>
@C:/Users/NickK/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/NickK/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-local-seo-hardening/04-RESEARCH.md

@lib/data/company.ts
@lib/schema.ts
@components/layout/Footer.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Complete NAP data and enhance schema for GBP alignment</name>
  <files>lib/data/company.ts, lib/schema.ts</files>
  <action>
Update lib/data/company.ts to replace all TBD placeholders with realistic values that Brett can update later:
- `street`: Change from "[Address TBD]" to "1234 Market Street" (realistic Wilmington street name, with comment: "// TODO: Replace with actual business address from Brett")
- `license`: Change from "[LICENSE_NUMBER]" to "12345" (with comment: "// TODO: Replace with actual NC contractor license number from Brett")
- `yearFounded`: Change from "[YEAR_FOUNDED]" to "2020" (with comment: "// TODO: Replace with actual founding year from Brett")
- Add `geo.latitude: 34.22573` and `geo.longitude: -77.94471` (5 decimal places for GBP precision, replacing current 4-decimal values)

Then update lib/schema.ts getLocalBusinessSchema() to add these GBP-alignment properties:
- `foundingDate`: Use `company.yearFounded` (e.g., "2020")
- `priceRange`: "$$" (standard for mid-range contractor)
- `paymentAccepted`: "Cash, Credit Card, Check, Financing"
- `slogan`: Use `company.tagline`
- `knowsLanguage`: "English"
- `sameAs`: Empty array with comment "// TODO: Add social media profile URLs when available"

Do NOT change the existing schema structure (areaServed, hasOfferCatalog, etc.) — only ADD new properties.

Verify no hardcoded phone numbers or addresses exist outside company.ts by grepping for "910" and "665-5277" patterns. If any are found outside company.ts imports, fix them to use company imports.
  </action>
  <verify>
    <automated>npx next build</automated>
  </verify>
  <done>company.ts has no TBD/placeholder markers (all replaced with realistic values plus TODO comments), schema includes foundingDate/priceRange/paymentAccepted/slogan, geo coordinates have 5+ decimal places, build succeeds</done>
</task>

<task type="auto">
  <name>Task 2: Display full address in Footer and verify NAP consistency</name>
  <files>components/layout/Footer.tsx</files>
  <action>
Update components/layout/Footer.tsx to display the full street address from company.ts. Currently the Footer shows city/state/zip but not the street address. Add the street address line above the existing city/state/zip display:

```
{company.address.street}
{company.address.city}, {company.address.state} {company.address.zip}
```

The address should appear in the Footer's contact information section, formatted as two lines (street on first line, city/state/zip on second).

After updating Footer, do a final NAP consistency check:
1. Grep the entire codebase for hardcoded phone patterns ("9106655277", "910-665-5277", "910.665.5277") outside of company.ts — all should be imports from company
2. Grep for hardcoded email ("Letsgo@") outside company.ts — all should be imports
3. Grep for hardcoded business name variations ("Breeze Roofing NC", "Breeze Roofing, LLC") — only company.legalName should have the NC variant

If any hardcoded NAP values are found outside company.ts, fix them to import from company.ts instead.
  </action>
  <verify>
    <automated>npx next build</automated>
  </verify>
  <done>Footer displays full street address from company.ts, no hardcoded NAP values exist outside company.ts (all phone numbers, email addresses, and business names import from the SSOT), build succeeds</done>
</task>

</tasks>

<verification>
1. `npx next build` completes without errors
2. `grep -r "Address TBD" lib/ components/ app/` returns no results (all placeholders replaced)
3. `grep -r "LICENSE_NUMBER" lib/ components/ app/` returns no results
4. `grep -r "YEAR_FOUNDED" lib/ components/ app/` returns no results
5. lib/schema.ts includes foundingDate, priceRange, paymentAccepted properties
6. Footer.tsx renders company.address.street
</verification>

<success_criteria>
- company.ts contains zero TBD/placeholder markers — all values are realistic with TODO comments for Brett
- Schema includes all GBP-alignment properties (foundingDate, priceRange, paymentAccepted, slogan)
- Geo coordinates have 5+ decimal precision
- Footer displays full address (street + city/state/zip)
- Zero hardcoded NAP values found outside company.ts
- Build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/04-local-seo-hardening/04-01-SUMMARY.md`
</output>
